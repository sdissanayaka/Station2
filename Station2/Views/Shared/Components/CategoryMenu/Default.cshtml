@model IEnumerable<ItemCategory>

<li class="dropdown">
    <a asp-controller="ItemMasterCustomer"
       asp-action="Index"
       class="dropdown-toggle" data-toggle="dropdown">Item Master<b class="caret"></b></a>
    <ul class="dropdown-menu">
        @foreach (var category in Model)
        {
            <li>
                <a asp-controller="ItemMasterCustomer" asp-action="List"
                   asp-route-category="@category.CategoryName">@category.CategoryName</a>
            </li>
        }
        <li class="divider"></li>
        <li>
            <a asp-controller="ItemMasterCustomer" asp-action="List" asp-route-Category="">View all Items</a>
        </li>
    </ul>
</li>